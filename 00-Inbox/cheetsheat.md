cd C:\xampp\htdocs

cd $env:LOCALAPPDATA\nvim

higlight all code :s/^/-- /

mini cheetsheet = g?

---

git clone https://github.com/jimbolol13/nvim-config nvim

git clone https://github.com/jimbolol13/nvim-config $env:LOCALAPPDATA\nvim
nvim

---

# doc

K = hover documentation

space fD = search understanding
space sD = example doc
space gD = search selected word in doc

# seach

space sf = search all files
space sn = seach all neovim config files
space n = seachr all notes

# window

ctrl w + v = split window verticaly
